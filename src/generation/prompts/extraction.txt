You are a data scientist extracting information to build a Knowledge Graph.

Analyze the following text and extract:
1. Key Entities (People, Organizations, Locations, Concepts, Dates).
2. Relationships between these entities.

Output strictly in JSON format with the following structure:
{{
    "entities": [
        {{"name": "Entity Name", "type": "PERSON/ORG/etc", "description": "Short summary"}}
    ],
    "relationships": [
        {{"source": "Entity Name", "target": "Other Entity Name", "relation_type": "WORKS_AT/LOCATED_IN/etc"}}
    ]
}}

Keep relationship types simple high-level/conceptual (e.g., RELATED_TO, PART_OF, LOCATED_AT).
Do not output any markdown formatting like ```json ... ```. Just the raw JSON.

Text to analyze:
{text}
